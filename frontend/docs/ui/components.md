# Komponentët e UI

Koleksion i komponentëve të ripërdorshme të ndërfaqes së përdoruesit.

## Button Components

### PrimaryButton

Butoni kryesor i aplikacionit me stilizim të Spotify.

```typescript\ninterface PrimaryButtonProps {\n  children: ReactNode;\n  onClick?: () => void;\n  disabled?: boolean;\n  loading?: boolean;\n  size?: 'sm' | 'md' | 'lg';\n  variant?: 'solid' | 'outline' | 'ghost';\n  leftIcon?: ReactElement;\n  rightIcon?: ReactElement;\n}\n\nfunction PrimaryButton({\n  children,\n  onClick,\n  disabled = false,\n  loading = false,\n  size = 'md',\n  variant = 'solid',\n  leftIcon,\n  rightIcon,\n  ...props\n}: PrimaryButtonProps) {\n  const baseClasses = 'inline-flex items-center justify-center font-medium rounded-full transition-all duration-200';\n  \n  const sizeClasses = {\n    sm: 'px-3 py-1.5 text-sm',\n    md: 'px-4 py-2 text-base',\n    lg: 'px-6 py-3 text-lg'\n  };\n  \n  const variantClasses = {\n    solid: 'bg-green-500 text-white hover:bg-green-400 active:bg-green-600',\n    outline: 'border-2 border-green-500 text-green-500 hover:bg-green-500 hover:text-white',\n    ghost: 'text-green-500 hover:bg-green-500 hover:bg-opacity-10'\n  };\n  \n  const disabledClasses = 'opacity-50 cursor-not-allowed';\n  \n  return (\n    <button\n      className={`\n        ${baseClasses}\n        ${sizeClasses[size]}\n        ${variantClasses[variant]}\n        ${disabled || loading ? disabledClasses : ''}\n      `}\n      onClick={onClick}\n      disabled={disabled || loading}\n      {...props}\n    >\n      {loading && (\n        <Spinner size={size === 'sm' ? 'xs' : 'sm'} className=\"mr-2\" />\n      )}\n      \n      {leftIcon && !loading && (\n        <span className=\"mr-2\">{leftIcon}</span>\n      )}\n      \n      {children}\n      \n      {rightIcon && (\n        <span className=\"ml-2\">{rightIcon}</span>\n      )}\n    </button>\n  );\n}\n\n// Përdorimi\n<PrimaryButton \n  onClick={handlePlay}\n  leftIcon={<FiPlay />}\n  loading={isLoading}\n>\n  Luaj Tani\n</PrimaryButton>\n```\n\n### IconButton\n\nButon vetëm me ikonë për veprime të shpejta.\n\n```typescript\ninterface IconButtonProps {\n  icon: ReactElement;\n  onClick?: () => void;\n  disabled?: boolean;\n  size?: 'sm' | 'md' | 'lg';\n  variant?: 'solid' | 'ghost';\n  'aria-label': string;\n}\n\nfunction IconButton({\n  icon,\n  onClick,\n  disabled = false,\n  size = 'md',\n  variant = 'ghost',\n  'aria-label': ariaLabel,\n  ...props\n}: IconButtonProps) {\n  const sizeClasses = {\n    sm: 'w-8 h-8 text-sm',\n    md: 'w-10 h-10 text-base',\n    lg: 'w-12 h-12 text-lg'\n  };\n  \n  return (\n    <button\n      className={`\n        inline-flex items-center justify-center rounded-full transition-all\n        ${sizeClasses[size]}\n        ${variant === 'solid' \n          ? 'bg-white text-black hover:scale-105' \n          : 'text-white hover:bg-white hover:bg-opacity-10'\n        }\n        ${disabled ? 'opacity-50 cursor-not-allowed' : ''}\n      `}\n      onClick={onClick}\n      disabled={disabled}\n      aria-label={ariaLabel}\n      {...props}\n    >\n      {icon}\n    </button>\n  );\n}\n\n// Përdorimi\n<IconButton \n  icon={<FiHeart />}\n  onClick={handleLike}\n  aria-label=\"Pëlqej këngën\"\n  variant=\"ghost\"\n/>\n```\n\n## Input Components\n\n### SearchInput\n\nInput i specializuar për kërkim.\n\n```typescript\ninterface SearchInputProps {\n  value: string;\n  onChange: (value: string) => void;\n  onSubmit?: (value: string) => void;\n  placeholder?: string;\n  loading?: boolean;\n  suggestions?: string[];\n  onSuggestionClick?: (suggestion: string) => void;\n}\n\nfunction SearchInput({\n  value,\n  onChange,\n  onSubmit,\n  placeholder = \"Kërko...\",\n  loading = false,\n  suggestions = [],\n  onSuggestionClick\n}: SearchInputProps) {\n  const [showSuggestions, setShowSuggestions] = useState(false);\n  const inputRef = useRef<HTMLInputElement>(null);\n  \n  const handleSubmit = (e: React.FormEvent) => {\n    e.preventDefault();\n    onSubmit?.(value);\n    setShowSuggestions(false);\n  };\n  \n  const handleSuggestionClick = (suggestion: string) => {\n    onChange(suggestion);\n    onSuggestionClick?.(suggestion);\n    setShowSuggestions(false);\n  };\n  \n  return (\n    <div className=\"relative w-full\">\n      <form onSubmit={handleSubmit}>\n        <div className=\"relative\">\n          <FiSearch className=\"absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400\" />\n          \n          <input\n            ref={inputRef}\n            type=\"text\"\n            value={value}\n            onChange={(e) => onChange(e.target.value)}\n            onFocus={() => setShowSuggestions(true)}\n            onBlur={() => setTimeout(() => setShowSuggestions(false), 200)}\n            placeholder={placeholder}\n            className=\"\n              w-full pl-10 pr-10 py-3 bg-gray-800 text-white rounded-full\n              border border-gray-600 focus:border-green-500 focus:outline-none\n              placeholder-gray-400\n            \"\n          />\n          \n          {loading && (\n            <Spinner \n              size=\"sm\" \n              className=\"absolute right-3 top-1/2 transform -translate-y-1/2\" \n            />\n          )}\n          \n          {value && !loading && (\n            <button\n              type=\"button\"\n              onClick={() => onChange('')}\n              className=\"absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-400 hover:text-white\"\n            >\n              <FiX />\n            </button>\n          )}\n        </div>\n      </form>\n      \n      {/* Suggestions Dropdown */}\n      {showSuggestions && suggestions.length > 0 && (\n        <div className=\"absolute top-full left-0 right-0 mt-1 bg-gray-800 border border-gray-600 rounded-lg shadow-lg z-50\">\n          {suggestions.map((suggestion, index) => (\n            <button\n              key={index}\n              onClick={() => handleSuggestionClick(suggestion)}\n              className=\"w-full px-4 py-2 text-left text-white hover:bg-gray-700 first:rounded-t-lg last:rounded-b-lg\"\n            >\n              <FiSearch className=\"inline mr-2 text-gray-400\" />\n              {suggestion}\n            </button>\n          ))}\n        </div>\n      )}\n    </div>\n  );\n}\n```\n\n### FormInput\n\nInput i përgjithshëm për forma.\n\n```typescript\ninterface FormInputProps {\n  label?: string;\n  value: string;\n  onChange: (value: string) => void;\n  type?: 'text' | 'email' | 'password' | 'number';\n  placeholder?: string;\n  required?: boolean;\n  error?: string;\n  disabled?: boolean;\n  leftIcon?: ReactElement;\n  rightIcon?: ReactElement;\n}\n\nfunction FormInput({\n  label,\n  value,\n  onChange,\n  type = 'text',\n  placeholder,\n  required = false,\n  error,\n  disabled = false,\n  leftIcon,\n  rightIcon\n}: FormInputProps) {\n  const inputId = useId();\n  \n  return (\n    <div className=\"w-full\">\n      {label && (\n        <label \n          htmlFor={inputId}\n          className=\"block text-sm font-medium text-white mb-2\"\n        >\n          {label}\n          {required && <span className=\"text-red-500 ml-1\">*</span>}\n        </label>\n      )}\n      \n      <div className=\"relative\">\n        {leftIcon && (\n          <div className=\"absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400\">\n            {leftIcon}\n          </div>\n        )}\n        \n        <input\n          id={inputId}\n          type={type}\n          value={value}\n          onChange={(e) => onChange(e.target.value)}\n          placeholder={placeholder}\n          required={required}\n          disabled={disabled}\n          className={`\n            w-full py-3 px-4 bg-gray-800 text-white rounded-lg\n            border transition-colors\n            ${leftIcon ? 'pl-10' : ''}\n            ${rightIcon ? 'pr-10' : ''}\n            ${error \n              ? 'border-red-500 focus:border-red-400' \n              : 'border-gray-600 focus:border-green-500'\n            }\n            ${disabled ? 'opacity-50 cursor-not-allowed' : ''}\n            focus:outline-none placeholder-gray-400\n          `}\n        />\n        \n        {rightIcon && (\n          <div className=\"absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-400\">\n            {rightIcon}\n          </div>\n        )}\n      </div>\n      \n      {error && (\n        <p className=\"mt-1 text-sm text-red-500\">\n          {error}\n        </p>\n      )}\n    </div>\n  );\n}\n```\n\n## Card Components\n\n### SongCard\n\nKarta për shfaqjen e këngëve.\n\n```typescript\ninterface SongCardProps {\n  song: Song;\n  onPlay?: (song: Song) => void;\n  onLike?: (song: Song) => void;\n  onAddToPlaylist?: (song: Song) => void;\n  isPlaying?: boolean;\n  isLiked?: boolean;\n  showActions?: boolean;\n}\n\nfunction SongCard({\n  song,\n  onPlay,\n  onLike,\n  onAddToPlaylist,\n  isPlaying = false,\n  isLiked = false,\n  showActions = true\n}: SongCardProps) {\n  const [showMenu, setShowMenu] = useState(false);\n  \n  return (\n    <div className=\"group relative bg-gray-800 p-4 rounded-lg hover:bg-gray-700 transition-all\">\n      {/* Cover Image */}\n      <div className=\"relative aspect-square mb-4\">\n        <img\n          src={song.cover_image_url || '/default-song.png'}\n          alt={song.title}\n          className=\"w-full h-full object-cover rounded-md\"\n        />\n        \n        {/* Play Button Overlay */}\n        <div className=\"absolute inset-0 flex items-center justify-center bg-black bg-opacity-50 opacity-0 group-hover:opacity-100 transition-opacity\">\n          <IconButton\n            icon={isPlaying ? <FiPause /> : <FiPlay />}\n            onClick={() => onPlay?.(song)}\n            aria-label={isPlaying ? \"Ndalo\" : \"Luaj\"}\n            variant=\"solid\"\n            size=\"lg\"\n          />\n        </div>\n        \n        {/* Playing Indicator */}\n        {isPlaying && (\n          <div className=\"absolute top-2 right-2\">\n            <div className=\"flex space-x-1\">\n              <div className=\"w-1 h-4 bg-green-500 animate-pulse\"></div>\n              <div className=\"w-1 h-4 bg-green-500 animate-pulse\" style={{ animationDelay: '0.1s' }}></div>\n              <div className=\"w-1 h-4 bg-green-500 animate-pulse\" style={{ animationDelay: '0.2s' }}></div>\n            </div>\n          </div>\n        )}\n      </div>\n      \n      {/* Song Info */}\n      <div className=\"mb-2\">\n        <h3 className=\"text-white font-medium truncate\" title={song.title}>\n          {song.title}\n        </h3>\n        <p className=\"text-gray-400 text-sm truncate\" title={song.artist}>\n          {song.artist}\n        </p>\n      </div>\n      \n      {/* Actions */}\n      {showActions && (\n        <div className=\"flex items-center justify-between\">\n          <div className=\"flex items-center space-x-2\">\n            <IconButton\n              icon={<FiHeart className={isLiked ? 'fill-current text-green-500' : ''} />}\n              onClick={() => onLike?.(song)}\n              aria-label={isLiked ? \"Hiq nga të pëlqyerat\" : \"Shto në të pëlqyerat\"}\n              size=\"sm\"\n            />\n            \n            <span className=\"text-xs text-gray-400\">\n              {formatDuration(song.duration_seconds || 0)}\n            </span>\n          </div>\n          \n          <div className=\"relative\">\n            <IconButton\n              icon={<FiMoreHorizontal />}\n              onClick={() => setShowMenu(!showMenu)}\n              aria-label=\"Më shumë opsione\"\n              size=\"sm\"\n            />\n            \n            {/* Context Menu */}\n            {showMenu && (\n              <div className=\"absolute right-0 top-full mt-1 bg-gray-700 border border-gray-600 rounded-lg shadow-lg z-10 min-w-48\">\n                <button\n                  onClick={() => {\n                    onAddToPlaylist?.(song);\n                    setShowMenu(false);\n                  }}\n                  className=\"w-full px-4 py-2 text-left text-white hover:bg-gray-600 first:rounded-t-lg\"\n                >\n                  <FiPlus className=\"inline mr-2\" />\n                  Shto në Playlist\n                </button>\n                \n                <button\n                  onClick={() => {\n                    // Handle download\n                    setShowMenu(false);\n                  }}\n                  className=\"w-full px-4 py-2 text-left text-white hover:bg-gray-600\"\n                >\n                  <FiDownload className=\"inline mr-2\" />\n                  Shkarko\n                </button>\n                \n                <button\n                  onClick={() => {\n                    // Handle share\n                    setShowMenu(false);\n                  }}\n                  className=\"w-full px-4 py-2 text-left text-white hover:bg-gray-600 last:rounded-b-lg\"\n                >\n                  <FiShare2 className=\"inline mr-2\" />\n                  Ndaj\n                </button>\n              </div>\n            )}\n          </div>\n        </div>\n      )}\n    </div>\n  );\n}\n```\n\n### PlaylistCard\n\nKarta për shfaqjen e playlist-eve.\n\n```typescript\ninterface PlaylistCardProps {\n  playlist: Playlist;\n  onPlay?: (playlist: Playlist) => void;\n  onClick?: (playlist: Playlist) => void;\n  showSongCount?: boolean;\n}\n\nfunction PlaylistCard({\n  playlist,\n  onPlay,\n  onClick,\n  showSongCount = true\n}: PlaylistCardProps) {\n  return (\n    <div \n      className=\"group bg-gray-800 p-4 rounded-lg hover:bg-gray-700 transition-all cursor-pointer\"\n      onClick={() => onClick?.(playlist)}\n    >\n      {/* Cover */}\n      <div className=\"relative aspect-square mb-4\">\n        <img\n          src={playlist.cover_image || '/default-playlist.png'}\n          alt={playlist.name}\n          className=\"w-full h-full object-cover rounded-md\"\n        />\n        \n        {/* Play Button */}\n        <div className=\"absolute inset-0 flex items-center justify-center bg-black bg-opacity-50 opacity-0 group-hover:opacity-100 transition-opacity\">\n          <IconButton\n            icon={<FiPlay />}\n            onClick={(e) => {\n              e.stopPropagation();\n              onPlay?.(playlist);\n            }}\n            aria-label=\"Luaj playlist-ën\"\n            variant=\"solid\"\n            size=\"lg\"\n          />\n        </div>\n      </div>\n      \n      {/* Info */}\n      <div>\n        <h3 className=\"text-white font-medium truncate mb-1\" title={playlist.name}>\n          {playlist.name}\n        </h3>\n        \n        {playlist.description && (\n          <p className=\"text-gray-400 text-sm truncate mb-1\" title={playlist.description}>\n            {playlist.description}\n          </p>\n        )}\n        \n        {showSongCount && (\n          <p className=\"text-gray-400 text-xs\">\n            {playlist.song_count || 0} këngë\n          </p>\n        )}\n      </div>\n    </div>\n  );\n}\n```\n\n## Loading Components\n\n### Spinner\n\n```typescript\ninterface SpinnerProps {\n  size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';\n  color?: string;\n  className?: string;\n}\n\nfunction Spinner({ size = 'md', color = 'currentColor', className = '' }: SpinnerProps) {\n  const sizeClasses = {\n    xs: 'w-3 h-3',\n    sm: 'w-4 h-4',\n    md: 'w-6 h-6',\n    lg: 'w-8 h-8',\n    xl: 'w-12 h-12'\n  };\n  \n  return (\n    <div\n      className={`animate-spin rounded-full border-2 border-transparent border-t-current ${sizeClasses[size]} ${className}`}\n      style={{ color }}\n      role=\"status\"\n      aria-label=\"Duke ngarkuar\"\n    >\n      <span className=\"sr-only\">Duke ngarkuar...</span>\n    </div>\n  );\n}\n```\n\n### LoadingSkeleton\n\n```typescript\nfunction LoadingSkeleton({ className = '' }: { className?: string }) {\n  return (\n    <div className={`animate-pulse bg-gray-700 rounded ${className}`}>\n      <div className=\"sr-only\">Duke ngarkuar përmbajtjen...</div>\n    </div>\n  );\n}\n\n// Skeleton për SongCard\nfunction SongCardSkeleton() {\n  return (\n    <div className=\"bg-gray-800 p-4 rounded-lg\">\n      <LoadingSkeleton className=\"aspect-square mb-4\" />\n      <LoadingSkeleton className=\"h-4 mb-2\" />\n      <LoadingSkeleton className=\"h-3 w-3/4\" />\n    </div>\n  );\n}\n```\n\n## Të Lidhura\n\n- [Styling Guide](../guides/styling.md)\n- [Component Patterns](../guides/component-patterns.md)\n- [Accessibility](../guides/accessibility.md)\n- [Testing Components](../guides/testing.md)